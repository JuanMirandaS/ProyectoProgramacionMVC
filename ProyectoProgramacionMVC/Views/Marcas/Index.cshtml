@model IEnumerable<ProyectoProgramacionMVC.Models.Marca>

@{
    ViewData["Title"] = "Marcas";
}

<div class="container">
    <div class="brand-list">
        <h2>Lista de Marcas</h2>

        <a asp-action="Create" class="btn btn-primary">Crear Nueva Marca</a>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(model => model.Nombre)</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.MarcaId" class="btn btn-warning">Editar</a>
                            <a asp-action="Details" asp-route-id="@item.MarcaId" class="btn btn-info">Detalles</a>
                            <a asp-action="Delete" asp-route-id="@item.MarcaId" class="btn btn-danger">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<style>
    .brand-list {
        background-color: #800080;
        padding: 20px;
        border-radius: 8px;
        color: white;
        width: 80%;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        margin-top: 20px;
    }

    .table th, .table td {
        color: white;
    }

    .btn {
        margin-right: 5px;
    }
</style>
